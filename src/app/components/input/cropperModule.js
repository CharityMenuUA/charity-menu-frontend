// Lazy-loaded chunk: react-cropper + its CSS. Imported on demand in
// ImageUpload so the ~80 KB cropperjs bundle only downloads after a user
// actually picks an image to edit.
import "cropperjs/dist/cropper.css"
import Cropper from "react-cropper"

export default Cropper
